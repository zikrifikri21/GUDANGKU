// import { useRealtime } from '@/components/realtime-provider';

export function ConnectionStatus() {
    return null;
    // const { isConnected, permission } = useRealtime();

    // if (!isConnected) {
    //     return null;
    // }

    // return (
    //     <div className="fixed bottom-4 right-4 z-50 flex items-center gap-2 rounded-lg border border-green-500/50 bg-green-500/10 px-3 py-2 text-xs text-green-600 dark:text-green-400">
    //         <span className="relative flex h-2 w-2">
    //             <span className="absolute inline-flex h-full w-full animate-ping rounded-full bg-green-400 opacity-75"></span>
    //             <span className="relative inline-flex h-2 w-2 rounded-full bg-green-500"></span>
    //         </span>
    //         <span>Realtime Connected</span>
    //         {permission === 'granted' && <span className="text-green-600/60">• Notifikasi Aktif</span>}
    //     </div>
    // );
}